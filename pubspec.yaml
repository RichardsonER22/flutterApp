name: futapp
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+4
  path_provider: ^2.0.0
  provider: ^6.0.0 # Para manejar el estado
  logger: ^2.0.0
  font_awesome_flutter: ^10.8.0
  


flutter:
  uses-material-design: true
